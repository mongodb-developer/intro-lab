# 🦸‍♀️ 为 `authors` 集合启用验证

在这个练习中，您将为 `authors` 集合定义一个 JSON 模型，将模型应用于集合，并通过插入一个不符合模型的文档来测试模型校验功能。

这是一个需要您编写代码的高级练习。如果您遇到困难并且正在参加一个现场会议，您可以请求室内的讲师帮助。

1. 首先打开 GitHub Codespace 中的 `server/src/schema-validation/apply-schema.ts` 文件，并取消注释第 41-61 行。
1. 完成标有 `// TODO` 注释的任务。
1. 再次执行脚本以将模型应用于 `authors` 集合。
    ```bash
    cd server
    node dist/schema-validation/apply-schema.js 
    ```
1. 最后，通过插入一个不符合模型的文档来测试模型验证。